"""
Module: tasks.apps
Description: Django application configuration for the tasks module.
"""

from django.apps import AppConfig


class TasksConfig(AppConfig):
    """Application configuration for the tasks module."""
    name = 'tasks'
