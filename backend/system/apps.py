"""
Module: system.apps
Description: Django application configuration for the system module.
"""

from django.apps import AppConfig


class SystemConfig(AppConfig):
    """Configures application metadata for the system app."""
    name = 'system'
